<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- HTTPS only -- see docs/security.md#transport-security; no cleartext traffic allowed. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".WpShareToDraftApplication"
        android:allowBackup="false"
        android:label="@string/app_name"
        android:theme="@style/Theme.WpShareToDraft"
        android:usesCleartextTraffic="false">

        <!--
        Single Activity, per docs/phase3-android-app-design.md#1-screen-transition-diagram:
        answers both a normal launcher intent (opens straight to Settings) and Chrome's
        share sheet (ACTION_SEND), hosting Settings/Confirm via Navigation Compose.
        -->
        <activity
            android:name=".presentation.share.ShareReceiverActivity"
            android:exported="true"
            android:theme="@style/Theme.WpShareToDraft">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>
    </application>

</manifest>
