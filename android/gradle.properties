org.gradle.jvmargs=-Xmx2048m
kotlin.code.style=official
android.useAndroidX=true
# AGP 8.5.2 was tested up to compileSdk 34; 35 works but AGP warns on every build.
# Acknowledging deliberately rather than leaving noise in CI logs -- revisit when
# bumping AGP to a version that lists 35 as tested.
android.suppressUnsupportedCompileSdk=35
