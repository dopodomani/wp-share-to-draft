org.gradle.jvmargs=-Xmx2048m
kotlin.code.style=official
android.useAndroidX=true
# AGP 8.9.3 was tested up to a lower compileSdk than 37, which this project now needs
# (see app/build.gradle.kts). Acknowledging deliberately rather than leaving noise in
# CI logs -- revisit when bumping AGP to a version that lists 37 as tested.
android.suppressUnsupportedCompileSdk=37
