# Force LF for the Gradle wrapper shell script regardless of the checkout platform's
# core.autocrlf setting -- a CRLF-converted shebang line breaks it on Unix (main PC/CI).
android/gradlew text eol=lf
android/gradlew.bat text eol=crlf
android/gradle/wrapper/gradle-wrapper.jar binary
